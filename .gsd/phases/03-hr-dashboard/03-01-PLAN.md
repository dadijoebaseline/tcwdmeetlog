---
phase: "03"
plan: "01"
name: "HR Dashboard - Meetings"
type: "feature"
autonomous: true
wave: 1
depends_on: "02-01"

objective: |
  HR can create meetings, manage attendees, assign attendees, generate QR codes, and see real-time attendance tracking.
  Build the core business dashboard for meeting management with full CRUD operations.

context_files:
  - ".gsd/PROJECT.md"
  - ".gsd/phases/01-foundation/SCHEMA.md"
  - ".gsd/STATE.md"

tasks:
  - name: "Create Meeting pages (create, list, view)"
    type: "auto"
    description: |
      Build meeting management pages for HR:
      - POST /dashboard/meetings/create - Form to create new meeting
      - GET /dashboard/meetings - List all meetings with filtering
      - GET /dashboard/meetings/[id] - View meeting details
      Save meetings to Firestore 'meetings' collection with fields:
      - id (auto), title, date, time, venue, description, createdBy, createdAt, attendees[], qrCode
    done: |
      - Create form component for meeting details (title, date, time, venue, description)
      - Meeting page lists all meetings with created date, attendee count, status
      - Detail page shows full meeting info with tabs (Info, Attendees, Attendance)
      - All meetings saved to Firestore 'meetings' collection

  - name: "Build attendee management UI"
    type: "auto"
    description: |
      Allow HR to add/remove attendees from a meeting:
      - Search and add attendees from user list
      - Bulk add via CSV upload (name, email)
      - Remove attendees from meeting
      - Show attendee list with check-in status
      Store in Firestore meetings/[id]/attendees array with {uid, name, email, checkedIn, checkInTime}
    done: |
      - Attendee tab in meeting detail shows list of assigned attendees
      - Add attendees button opens modal with user search
      - Shows attendee names, email, current check-in status
      - Remove button deletes attendee from meeting
      - CSV upload accepts columns: name, email (optional feature)

  - name: "Implement QR code generation"
    type: "auto"
    description: |
      Generate unique QR code for each meeting:
      - Use qrcode.react library
      - QR payload format: {meetingId, meetingTitle, timestamp}
      - Generate on meeting creation
      - Display QR on meeting detail page
      - Provide download/print options
      Store QR code data URL in Firestore meetings.qrCode field
    done: |
      - Meeting detail page shows large QR code display
      - QR code generated using qrcode.react
      - Contains meeting ID and title in payload
      - Download as PNG button
      - Print button for QR code
      - QR data stored in Firestore

  - name: "Create real-time attendance display"
    type: "auto"
    description: |
      Show attendance status updates in real-time:
      - Table of attendees with columns: Name, Email, Check-in Status, Check-in Time
      - Use Firestore listener for real-time updates
      - Show "Pending" / "Checked In" / "Absent" status
      - Calculate attendance percentage
      - Highlight recently checked-in attendees
    done: |
      - Attendance tab in meeting detail shows attendee table
      - Real-time updates as attendees check in
      - Columns: Name, Email, Status (Pending/Checked In), Check-in Time
      - Attendance percentage displayed at top
      - Green highlight for checked-in attendees
      - Firestore listener updates UI on document changes

  - name: "Build meeting dashboard with KPIs"
    type: "auto"
    description: |
      Update main HR dashboard with meeting-focused KPIs:
      - Total meetings this month
      - Today's meetings count
      - Total unique attendees attended this month
      - Average attendance rate %
      - Quick links to recent meetings
      Queries to Firestore meetings collection with where() constraints
    done: |
      - Dashboard shows 4 KPI cards at top
      - Meeting list showing today's and upcoming meetings
      - Recent meetings widget (last 5 meetings)
      - Each KPI calculated from Firestore data
      - Click through to meeting details

  - name: "Add meeting archive/history"
    type: "auto"
    description: |
      Track past meetings separately from active:
      - Complete a meeting to archive (mark as archived)
      - View archived meetings in history tab
      - Generate attendance summary for past meetings
      - Export meeting data (attended list)
      Use Firestore query with status field (active/archived)
    done: |
      - Meeting list shows "Active" and "Past" tabs
      - Past meetings show attendance summary
      - Download attendance button exports meeting attendees list
      - Meeting detail shows completion date for past meetings

  - name: "Implement meeting filters and search"
    type: "auto"
    description: |
      Allow HR to find meetings easily:
      - Search by meeting title
      - Filter by date range
      - Filter by status (active/archived)
      - Sort by created date, date, attendee count
    done: |
      - Meeting list page has search box and filter controls
      - Search filters by title in real-time
      - Date range picker for filtering
      - Status filter dropdown
      - Sort options working correctly

  - name: "Build meeting edit/delete functionality"
    type: "auto"
    description: |
      Allow HR to modify or remove meetings:
      - Edit page for meeting details (title, date, time, venue)
      - Delete meeting with confirmation modal
      - Archive meeting instead of delete (soft delete)
      Validation: Can't modify meeting with existing check-ins
    done: |
      - Edit button on meeting detail opens edit form
      - Edit form pre-populated with current data
      - Save/Cancel buttons with validation
      - Delete button with confirmation modal
      - Success/error messages after save/delete
      - Redirect to meeting list on success

verification:
  - "HR can create a new meeting and see it in the list"
  - "QR code displays on meeting detail and can be downloaded"
  - "Attendees can be added to meetings and show in attendee list"
  - "Real-time attendance updates (mock/manual updates work)"
  - "Dashboard KPIs display correct numbers"
  - "Meetings can be edited and deleted"
  - "Search and filter controls work correctly"
  - "Navigation between meeting pages is smooth"
  - "All data persists in Firestore"

success_criteria:
  - "All 8 tasks completed and committed"
  - "No TypeScript errors or build warnings"
  - "All pages render correctly with Tailwind styling"
  - "Firestore writes succeed for meetings and attendees"
  - "Real-time Firestore listeners working (mock check-ins)"
  - "QR codes generate and display correctly"
  - "App builds and deploys without errors"

output:
  summary: ".gsd/phases/03-hr-dashboard/03-01-SUMMARY.md"
  files:
    created:
      - "app/dashboard/meetings/page.tsx"
      - "app/dashboard/meetings/create.tsx"
      - "app/dashboard/meetings/[id]/page.tsx"
      - "app/dashboard/meetings/[id]/edit.tsx"
      - "lib/meeting-service.ts"
      - "components/QRCodeDisplay.tsx"
      - "components/MeetingForm.tsx"
    modified:
      - "app/dashboard/page.tsx"
      - "lib/auth-context.tsx"
